enable: false
license: free

# If set to 0, there is no limit
limit: 6

# Database support
# - sqlite
# - mysql
# - postgresql
database:
  type: sqlite

  sqlite:
    path: party.db

  mysql:
    host: localhost
    port: "3306"
    name: mcengine
    user: root
    password: ""

  postgresql:
    host: localhost
    port: "5432"
    name: mcengine
    user: postgres
    password: ""
